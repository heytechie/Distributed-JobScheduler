services:
  db:
    image: postgres:16-alpine
    container_name: job_orchestrator_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: job_orchestrator
    ports:
      - "5433:5432"
  